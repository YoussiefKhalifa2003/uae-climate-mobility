# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
websockets==14.1
httpx==0.28.1
python-dotenv==1.0.1

# --- Geospatial ---
osmnx==2.0.1
geopandas==1.0.1
shapely==2.0.6
networkx==3.4.2
rasterio==1.4.3
pyproj==3.7.0
pyarrow==18.1.0

# --- Science / numerics ---
numpy==2.1.3
scipy==1.14.1
pvlib==0.11.2
pythermalcomfort==2.10.0

# --- Testing ---
pytest==8.3.4

# --- GPU (optional, install manually to match your CUDA) ---
# RTX 4080 Super with CUDA 12.x:
#   pip install cupy-cuda12x==13.3.0
# Everything has a NumPy CPU fallback, so this is optional.
